export const ROLES = ["adopter", "reporter", "admin"];
// Roles a person may pick for themselves. "admin" can only be granted by an existing admin (or the seed script).
export const SELF_ROLES = ["adopter", "reporter"];
